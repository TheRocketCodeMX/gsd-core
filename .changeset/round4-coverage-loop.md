---
type: Fixed
pr: 67
---

**Round-4 convergence polish for the certification + suite-health machinery.** The coverage-gap feedback loop is now mechanically closed rather than a documented handoff: `/gsd:add-tests` reads TEST-STRATEGY.md's `## Coverage debt` ledger, treats each open row's named escaped behavior as a first-class prioritized input (written at the level the row prescribes, not re-derived from a changed-file scan that already missed it), and transitions the row `open → closed` with the closing test reference once its test passes — giving the Status column a real lifecycle it never had. The certification substrate gains its missing fourth policy: an authoritative `## Seed accounts` section (idempotent seed script, role-tagged accounts documented in TEST-STRATEGY, credentials in the env/secret store and never in the repo, deterministic identifiers routed to the sandbox mail catcher), which resolves a dangling `(§ seed accounts)` cross-reference and completes the substrate list that previously named only three of four policies. And the vendor-fact verbatim-emit exemption's comment is corrected to state it is `certification.md`-specific and does not auto-generalize — a future vendor-fact reference must be added explicitly, with the fail-safe that a forgotten one falls through to the brand transform and is caught by the emitted-fidelity test rather than leaking un-transformed.
