{
  "version": 1,
  "paths": {
    "verify-work.md": "Round-2 Wave A (e2e-6 F4/F8), atop the Wave-2 growth: restart is defined (archive to -UAT-superseded-{date}.md, never clobber, offer names what is archived); create_uat_file documents the [pending-certifier] state for CERT-2 handovers and the pending / declined-with-reason line grammars. Cumulative +1148 bytes vs next. Round-2 Wave B (e2e-4 F5/F7): the human_needed\u2192passed stamp became CONDITIONAL on behavior_unverified == 0 AND no [pending-certifier]/could-not-prove checkpoint (it previously produced a file reading `status: passed` beside `behavior_unverified: 1`), with the withheld-stamp presentation naming what is unproven; and coverage_gap_capture gained a record-only entry so a gap the verifier or certifier named still writes its `## Coverage debt` row when the human reported zero UAT issues \u2014 the only writer of that section was gated behind the one event that did not happen. | Round-3: grew +2848B (e2e-8 F1/F2/F3: complete_session defers to uat_blockers; \u00a71.6 untrusted-result validation; return-side always-escalate)",
    "execute-plan.md": "feat/testing-certification Wave 2 (spec \u00a75, evidence schema): a clause APPENDED TO THE FIRST EXISTING `coverage:` authoring bullet (deliberately not a new bullet: `tests/no-bare-gsd-tools-command-position.test.cjs` pins an allowlisted prose mention at execute-plan.md:387 BY LINE NUMBER, so any inserted line breaks it \u2014 see the FORK-DELTA row, which records the same constraint) describing the `kind: agentic_certification` evidence entry \u2014 its ref is the certification run's evidence bundle (transcript plus captures where the driver was probed capable), never a test path; executors do not author these (verify-work's certification step writes them back), and never file a certification claim you did not run. 36054 -> 37281 bytes. The matching enum change lands in gsd-core/templates/summary.md and src/coverage.cts; pinned by tests/testing-certification.test.cjs and tests/coverage-metadata-parser.test.cjs. This file's SPENT #2649 entry (in tests/emitted-drift-acks/2649-diagnose-execute-plan-base-check.json, already absorbed at the base and therefore inert) was deleted in the same commit, because two ack sources may never name the same path. Wave 3 adds one further paragraph to the same `create_summary` step: the `suite-metrics:` authoring contract (record test_count/wall_clock/containers_started only when this plan ran the WHOLE suite; otherwise omit, because the phase's post-merge test gate records it instead; never estimate and never write 0 for not-measured, since transition's suite-health compare reads the block against the TEST-STRATEGY baseline). It is placed AFTER line 387 for the same PROSE_ALLOWLIST reason recorded above, and a contract test now pins that line so a future edit cannot silently shift it again: +790 bytes. | Round-3: grew +196B (e2e-9 F5: wall_clock seconds -> wall_clock_ms)"
  }
}
