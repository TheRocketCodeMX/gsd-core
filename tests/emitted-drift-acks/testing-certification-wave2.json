{
  "version": 1,
  "paths": {
    "verify-work.md": "feat/testing-certification Wave 2 (docs/superpowers/specs/2026-08-10-testing-certification-design.md §5, §7): the verify loop gains the two things the strategy decision had nothing to act on. (1) An `agentic-certification` gsd:section gate placed between `extract_tests` and `create_uat_file` — after the checkpoint set exists (the brief is generated from `present[]`) and before any checkpoint is presented — dispatching the fork-owned gsd-core/workflows/verify-work/steps/agentic-certification.md, plus a short block in `create_uat_file` that writes the certified `source: agentic` entries and the single certification outcome line. The host carries only the dispatch line, the placement rationale, and that write block; all of the certification mechanics — trust gate, capability re-check, brief generation, escalation rules, builder-not-certifier separation, the outcome table — live in the extracted step file, NOT here. (2) A `coverage_gap_capture` step between `diagnose_issues` and `plan_gap_closure` implementing the §7 feedback loop: the \"which fast test was missing\" question, its routes (add-tests for the test, the existing gap route for the code fix), and the append-only write into TEST-STRATEGY.md's new `## Coverage debt` section. 38971 -> 42947 bytes, well under the LARGE tier cap (61440). Pinned by tests/testing-certification.test.cjs. Wave 2 review N1 (folded into Wave 3): the `gsd:section id=\"agentic-certification\"` wrapper is REMOVED. It was the only `when: \"always\"` entry in the whole section manifest, it bought nothing over the corpus's plain unconditional step-file dispatch (execute-phase.md -> post-merge-gate.md, which ships with no manifest entry), and it put a written-down silent-skip branch (\"Otherwise skip — do not read the file\") on disk in the one step whose contract is \"recorded, never silent\". The dispatch is now the plain form plus the placement rationale and the reason it is deliberately not a section; the manifest drops from 38 sections to 37 and docs/reference/workflow-fragments.md's \"verify-work.md marks two sections\" is correct again without an edit. Wave 2 review T4 (folded): the step file's posture now rides `certification_mode` on the init.verify-work bundle, so verify-work.md names the field it threads through. The dispatch paragraph avoids the literal `gsd:section` token in prose: tests/mcp-catalog-parity.install.test.cjs treats that substring as the marker whose absence proves composition ran, so a prose mention in a composed workflow makes the real installer and the served catalog appear to disagree. 2.4.1 e2e fix wave (e2e-2): cold-start pattern matching defined as basename-exact with the disambiguating example (F3 — the ambiguity fed the certification brief), capsule-added checkpoints named in create_uat_file on every tier including CERT-0, and the positional-phase-arg warning at the init call site (F6). +340 bytes.",
    "execute-plan.md": "feat/testing-certification Wave 2 (spec §5, evidence schema): a clause APPENDED TO THE FIRST EXISTING `coverage:` authoring bullet (deliberately not a new bullet: `tests/no-bare-gsd-tools-command-position.test.cjs` pins an allowlisted prose mention at execute-plan.md:387 BY LINE NUMBER, so any inserted line breaks it — see the FORK-DELTA row, which records the same constraint) describing the `kind: agentic_certification` evidence entry — its ref is the certification run's evidence bundle (transcript plus captures where the driver was probed capable), never a test path; executors do not author these (verify-work's certification step writes them back), and never file a certification claim you did not run. 36054 -> 37281 bytes. The matching enum change lands in gsd-core/templates/summary.md and src/coverage.cts; pinned by tests/testing-certification.test.cjs and tests/coverage-metadata-parser.test.cjs. This file's SPENT #2649 entry (in tests/emitted-drift-acks/2649-diagnose-execute-plan-base-check.json, already absorbed at the base and therefore inert) was deleted in the same commit, because two ack sources may never name the same path. Wave 3 adds one further paragraph to the same `create_summary` step: the `suite-metrics:` authoring contract (record test_count/wall_clock/containers_started only when this plan ran the WHOLE suite; otherwise omit, because the phase's post-merge test gate records it instead; never estimate and never write 0 for not-measured, since transition's suite-health compare reads the block against the TEST-STRATEGY baseline). It is placed AFTER line 387 for the same PROSE_ALLOWLIST reason recorded above, and a contract test now pins that line so a future edit cannot silently shift it again: +790 bytes."
  }
}
