{
  "version": 1,
  "paths": {
    "verify-work.md": "feat/testing-certification Wave 2 (docs/superpowers/specs/2026-08-10-testing-certification-design.md §5, §7): the verify loop gains the two things the strategy decision had nothing to act on. (1) An `agentic-certification` gsd:section gate before UAT — a sibling of the existing `automated-ui-verification` gate — dispatching the fork-owned gsd-core/workflows/verify-work/steps/agentic-certification.md; the host carries only the dispatch line plus the one-sentence rationale for `when=\"always\"` (every phase must leave a RECORDED certification outcome, so an init-time state gate would make the common case a silent skip). All of the certification mechanics — trust gate, capability re-check, brief generation, escalation rules, builder-not-certifier separation, the outcome table — live in the extracted step file, NOT here. (2) A `coverage_gap_capture` step between `diagnose_issues` and `plan_gap_closure` implementing the §7 feedback loop: the \"which fast test was missing\" question, its routes (add-tests for the test, the existing gap route for the code fix), and the append-only write into TEST-STRATEGY.md's new `## Coverage debt` section. 38971 -> 41529 bytes (+2558), well under the LARGE tier cap (61440). Pinned by tests/testing-certification.test.cjs.",
    "execute-plan.md": "feat/testing-certification Wave 2 (spec §5, evidence schema): one bullet added to the `coverage:` authoring guidance describing the `kind: agentic_certification` evidence entry — its ref is the certification run's evidence bundle (transcript plus captures where the driver was probed capable), never a test path; executors do not author these (verify-work's certification step writes them back), and never file a certification claim you did not run. 36054 -> 36549 bytes (+495). The matching enum change lands in gsd-core/templates/summary.md and src/coverage.cts; pinned by tests/testing-certification.test.cjs and tests/coverage-metadata-parser.test.cjs. This file's SPENT #2649 entry (in tests/emitted-drift-acks/2649-diagnose-execute-plan-base-check.json, already absorbed at the base and therefore inert) was deleted in the same commit, because two ack sources may never name the same path."
  }
}
